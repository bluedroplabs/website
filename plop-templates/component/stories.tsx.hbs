import type { Meta, StoryObj } from "@storybook/nextjs-vite";
import { {{pascalCase name}} } from "./{{pascalCase name}}";
import { {{constantCase name}}_EXAMPLE_PROPS } from "./{{pascalCase name}}.examples";

const meta: Meta<typeof {{pascalCase name}}> = {
  title: "{{properCase type}}s/{{titleCase name}}",
  component: {{pascalCase name}},
  tags: ["autodocs"],
};

export default meta;

type Story = StoryObj<typeof {{pascalCase name}}>;

export const Default: Story = { args: {{constantCase name}}_EXAMPLE_PROPS.default };
export const Secondary: Story = { args: {{constantCase name}}_EXAMPLE_PROPS.secondary };
