import type { IButton } from "./Button.types";

const BUTTON_DEFAULT_PROPS: IButton = {
  children: "Button",
  variant: "default",
};

export const BUTTON_EXAMPLE_PROPS = {
  default: BUTTON_DEFAULT_PROPS,
};
