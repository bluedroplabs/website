import type { HTMLAttributes } from "react";

const DOTTED_BACKGROUND_DEFAULT_PROPS: HTMLAttributes<HTMLElement> = {
  className: "h-screen flex pt-10 justify-center",
};

export const DOTTED_BACKGROUND_EXAMPLE_PROPS = {
  default: DOTTED_BACKGROUND_DEFAULT_PROPS,
};
